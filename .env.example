# Option 1: Using relative same-origin proxy (Recommended for both Vercel and Localhost)
VITE_API_BASE_URL=/QUEST

# Option 2: Direct backend URL (if configuring full domain in Vercel Environment Variables)
# VITE_API_URL=https://nexus-production-7fa0.up.railway.app/QUEST
